[project]
name = "ocr-research"
version = "0.1.0"
description = "OCR benchmark suite for metadata and table extraction"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mistralai>=2.9.3",
    "numpy",
    "paddleocr>=3.7",
    "paddlepaddle",
    "paddlex[ocr]",
    "pymupdf",
    "pypdf",
    "requests",
]

[project.optional-dependencies]
azure = ["azure-ai-documentintelligence", "azure-core"]
mistral = ["mistralai", "pymupdf"]
glm = ["pypdf"]
olmocr = ["olmocr"]
monkeyocr = ["requests"]
eval = ["rapidfuzz"]
cloud = [
    "azure-ai-documentintelligence",
    "azure-core",
    "mistralai",
    "pymupdf",
    "pypdf",
    "rapidfuzz",
]
all = [
    "azure-ai-documentintelligence",
    "azure-core",
    "mistralai",
    "olmocr",
    "pymupdf",
    "pypdf",
    "requests",
    "rapidfuzz",
]
